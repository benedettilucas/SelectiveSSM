## Models include

- [x] **MambaVision**
- [ ] VisionMamba
- [ ] VMamba

> The scan compilation feature is not avaiable yet. Needs install using 
```
!pip install ninja --only-binary ninja
!pip install torch==2.7 torchvision torchaudio
!pip install --no-cache-dir --no-binary :all: --no-build-isolation mamba-ssm
```